#include "ui_input_box.h"

#include "../font_manager.h"

void UiInputBox::initImpl() {
//	auto lTextRect = FontManager::optimalSizeForText( mSize->value, fm(), mTitle, mFontSize );
//	alignOn( lTextRect );
}
